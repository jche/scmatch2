% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate.R
\name{get_measurement_error_variance_alt}
\alias{get_measurement_error_variance_alt}
\title{Alternative measurement error variance estimator (hat_V_E_alt)}
\usage{
get_measurement_error_variance_alt(
  matches_table,
  df = NULL,
  outcome = "Y",
  treatment = "Z",
  use_common_variance = TRUE,
  K = 1,
  covs = NULL,
  scaling = NULL,
  metric = "maximum",
  id_name = "id"
)
}
\arguments{
\item{matches_table}{Full matched table (output of full_unit_table).}

\item{df}{Full original data frame.  Required when
\code{use_common_variance = FALSE}.}

\item{outcome}{Name of outcome variable (default "Y").}

\item{treatment}{Name of treatment variable (default "Z").}

\item{use_common_variance}{If TRUE (default), estimate S1^2 from the
control-side subclass variances (assuming sigma_1(x)=sigma_0(x)).
If FALSE, estimate S1^2 via treated-to-treated K-NN matching.}

\item{K}{Number of treated neighbours for the treated-to-treated step
(used only when \code{use_common_variance = FALSE}).}

\item{covs}{Character vector of covariate names, or NULL to auto-detect
columns starting with "X".}

\item{scaling}{Scaling vector for distance computation.}

\item{metric}{Distance metric (default "maximum").}

\item{id_name}{Name of the unit ID column (default "id").}
}
\value{
A list with elements V_E_alt, S0_sq, S1_sq, s_j_sq (data frame with
id/w_j/s_j_sq), s_t_sq, s_1t_sq (NULL when use_common_variance=TRUE),
cov_w_s, N_T, ESS_C.
}
\description{
Implements the plug-in estimator
hat_V_E_alt = S1^2 / n_T  +  S0^2 / ESS(C)
where S0^2 is the w_j^2-weighted average of s_j^2 across control units, and
S1^2 is either the simple average of s_t^2 (common-variance assumption) or
the average of s_{1t}^2 from treated-to-treated K-NN matching.
}
\details{
Also returns the empirical covariance Cov_p(w_j, s_j^2):
hat_V_E_alt = S^2*(1/n_T + 1/ESS_C) + (1/n_T)*Cov_p(w_j, s_j^2)
}
