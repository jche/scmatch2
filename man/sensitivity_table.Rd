% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity_plot.R
\name{sensitivity_table}
\alias{sensitivity_table}
\title{Make sensitivity table of different ATT estimates}
\usage{
sensitivity_table(
  csm,
  outcome = NULL,
  feasible_only = FALSE,
  include_variances = FALSE,
  include_distances = TRUE
)
}
\arguments{
\item{csm}{A csm_matches object}

\item{outcome}{Name of outcome variable in data}
}
\value{
A tibble with the four ATT estimates and standard errors
}
\description{
Given a csm_matches object, this function computes several different
ATT estimates:
\itemize{
\item FATT: ATT using only feasible treated units (i.e., those with
matches within the caliper)
\item ATT: ATT using all treated units, if there were adaptive calipers.
\item ATT_1nn: ATT using nearest-neighbor controls (if tied, use all tied controls).
\item ATT_raw: ATT, averaging all controls within each matched set.
\item ATT_raw_feas: ATT, averaging all controls within each matched set, but only for feasible treated units.
}
}
\details{
These estimates can be used to assess sensitivity of results to
different choices in matching and estimation.  Return results are from the \code{estimate_ATT()} method
}
\seealso{
\code{\link[=estimate_ATT]{estimate_ATT()}}
}
